# Installer skeleton

这一目录是 AIPanel 一句话安装器的代码骨架。

当前状态：

- 已有最小状态模型
- 已有阶段枚举
- 尚未接入真实 Feishu / Vercel 实现

当前目标不是直接跑通安装器，而是先把 repo 内的 installer contract 固化下来，避免后续把实现散落到 skill、脚本和文档里。

设计文档见：

- `docs/product/aipanel-one-command-installer-plan.md`
- `docs/product/aipanel-installer-phase-1.md`
